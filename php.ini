; Project-level PHP configuration overrides
; Used by: php artisan serve (via php -c php.ini artisan serve)
; For production (php-fpm/apache), apply these values in the system php.ini:
;   /etc/php/8.4/fpm/php.ini
;   /etc/php/8.4/apache2/php.ini
; Then restart: sudo systemctl restart php8.4-fpm   (or: sudo service apache2 restart)

upload_max_filesize = 12M
post_max_size = 14M
